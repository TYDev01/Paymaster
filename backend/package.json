{
  "name": "@paymaster/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sponsorship backend: signature engine, policy engine, chain adapters, and APIs.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.4"
  }
}
